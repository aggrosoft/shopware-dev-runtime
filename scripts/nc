#!/usr/bin/env bash
set -euo pipefail

if [[ $# -ne 2 ]]; then
    printf 'usage: nc HOST PORT\n' >&2
    exit 2
fi

host="$1"
port="$2"

exec 3<>"/dev/tcp/$host/$port"

cat <&3 &
reader_pid=$!

cat >&3 || true
wait "$reader_pid"
